<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Index</title>
    </head>
    <body>
        <% @recipes.each do |recipe| %>
                <p>Name: <a href="/recipes/<%= recipe.id %>"><%= recipe.name %></a></p>                        
        <% end %>
    </body>
</html>
