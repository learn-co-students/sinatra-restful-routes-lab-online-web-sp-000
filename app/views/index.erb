<!doctype html>
<html>
  <head>
    <title>Sinatra RESTful Routes</title>
  </head>
  <body>
    <ul>
        <% @recipes.each do |recipe|%>
          <p> <a href="/recipes/<%= recipe.id %>"><%= recipe.name%> </a> </p><br>

        <% end %>
    </ul>
  </body>
</html>